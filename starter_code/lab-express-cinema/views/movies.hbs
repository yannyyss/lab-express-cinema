<div id="container">
    {{#each movies}}
    <div class="pelicula">
        <img src={{this.image}}>
        <h3>{{this.title}}</h3>
        <a href="movies/{{this._id}}">See more</a>
    </div>
    {{/each}}
</div>